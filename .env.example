# Port for the admin server (default: 8289)
PORT=8289

# Secret for signing session cookies. Generate with: openssl rand -base64 32
SESSION_SECRET=change-me-in-production

# Set to true when behind HTTPS (e.g. Nginx + SSL). Leave unset or false for http://IP:8289.
# COOKIE_SECURE=true

# Optional: directory for JSON data (users.json, apps.json). Default: server/data
# DATA_DIR=./data

# First-time setup: create admin user (only works when no users exist)
# Set these then POST /api/auth/setup once, then remove or leave unset.
# ADMIN_USERNAME=admin
# ADMIN_PASSWORD=your-secure-password
